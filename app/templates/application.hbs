{{partial "application/navbar"}}

<div class="container-fluid">
  <div class="row">
    {{partial "application/sidebar"}}

    <section class="col-md-10">
      <button {{action "heal"}} class="btn btn-primary">
        Heal
      </button>

      <hr />

      {{outlet}}
    </section>
  </div>
</div>
